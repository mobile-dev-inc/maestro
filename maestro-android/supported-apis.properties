# Android API levels the Maestro driver supports. `max` trails compileSdk and moves only
# after test-e2e is green there (bump-android-version). Read by test-e2e.yaml and by the
# copilot device-readiness harness through the maestro submodule. Keep it key=value.
min=29
max=36
