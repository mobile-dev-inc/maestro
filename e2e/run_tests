#!/usr/bin/env sh
set -eu

# Runs all tests in demo_app/.maestro and the workspaces directory.
#
# Honors two optional env vars set by the workflow's validate-inputs job:
#   MAESTRO_APP        - restrict to a single workspace (e.g. demo_app)
#   MAESTRO_FLOW_PATH  - run only one resolved flow yaml (single-flow mode)

command -v maestro >/dev/null 2>&1 || { echo "maestro is required" && exit 1; }

[ "$(basename "$PWD")" = "e2e" ] || { echo "must be run from e2e directory" && exit 1; }

ALL_PASS=true

_h1() { printf "=>\n=> %s\n=>\n" "$1"; }
_h2() { printf "==> [%s] %s\n" "$1" "$2"; }
_h3() { printf "==> [%s] [%s] => %s\n" "$1" "$2" "$3"; }

cloud="android_device_configuration,ios_device_configuration" # Maestro Cloud specific tests
platform="${1:-}"
# platform_args carries whatever that platform needs on every maestro invocation. Web runs
# headless because CI has no display; the device platforms need nothing.
case "$platform" in
	android) exclude_tags="ios,web,$cloud";     platform_args="" ;;
	ios)     exclude_tags="android,$cloud,web"; platform_args="" ;;
	web)     exclude_tags="android,ios,$cloud"; platform_args="--headless" ;;
	*)       echo "usage: $0 <android|ios|web>"; exit 1 ;;
esac

app_filter="${MAESTRO_APP:-}"
flow_path="${MAESTRO_FLOW_PATH:-}"

# Everything the suite knows about each workspace — where its flows are, which platforms it
# runs on, whether it carries failing-tagged flows — lives in workspaces.txt, so adding or
# retiring one is a data change rather than an edit to the branches below.
manifest="workspaces.txt"
[ -f "$manifest" ] || { echo "$manifest not found" && exit 1; }

# Everything the suite knows about each workspace lives in workspaces.txt; validate_workspaces
# checks its shape and names any line that is wrong.
./validate_workspaces || exit 1

# Is $2 one of the comma-separated platforms in $1?
#
# `case X in PATTERN)` matches the word on the left against the pattern on the right, so the
# list is the subject here and the platform is what we search for inside it — the opposite way
# round from how the keyword `in` reads. Wrapping both in commas makes it a whole-word match,
# so "web" cannot match a "webview" entry, or vice versa.
_supports_platform() {
	platform_list="$1"
	looking_for="$2"

	case ",$platform_list," in
		*",$looking_for,"*) return 0 ;;
		*)                  return 1 ;;
	esac
}

fixtures_pid=""

_cleanup() {
	rm -f pipe
	# Only ours: a server that was already running when we started is left alone.
	if [ -n "$fixtures_pid" ]; then
		kill "$fixtures_pid" 2>/dev/null || true
	fi
}

mkfifo pipe
trap _cleanup EXIT


export MAESTRO_EXAMPLE="test-value"  # Relied upon in a test

_run_passing() {
	app_name="$1"
	workspace_target="$2"

	_h2 "$app_name" "run passing tests"

	while IFS= read -r line; do
		_h3 "$app_name" "passing" "$line"
	done < pipe &

	# shellcheck disable=SC2086  # platform_args must word-split, and vanish when empty
	maestro --verbose --no-ansi --platform "$platform" test --flatten-debug-output --debug-output="$HOME/.maestro/tests/${app_name}/passing" $platform_args --include-tags passing --exclude-tags "$exclude_tags" "$workspace_target" 1>pipe 2>&1 \
		|| { _h2 "$app_name" "FAIL! Expected all pass, but at least some failed instead"; ALL_PASS=false; }
}

_run_failing() {
	app_name="$1"
	workspace_target="$2"

	_h2 "$app_name" "run failing tests"

	while IFS= read -r line; do
		_h3 "$app_name" "failing" "$line"
	done < pipe &

	# Inverted polarity: maestro is EXPECTED to exit non-zero. We use `if maestro …` so that
	# (a) set -e doesn't abort the script when maestro correctly fails, and
	# (b) we record FAIL only when maestro succeeds (i.e. failing flows unexpectedly passed).
	# shellcheck disable=SC2086  # platform_args must word-split, and vanish when empty
	if maestro --verbose --no-ansi --platform "$platform" test --flatten-debug-output --debug-output="$HOME/.maestro/tests/${app_name}/failing" $platform_args --include-tags failing --exclude-tags "$exclude_tags" "$workspace_target" 1>pipe 2>&1; then
		_h2 "$app_name" "FAIL! Expected all to fail, but at least some passed instead"
		ALL_PASS=false
	fi
}

_run_single_flow() {
	app_name="$1"
	target_yaml="$2"

	_h1 "run single flow \"$target_yaml\" for app \"$app_name\" on platform \"$platform\""
	_h2 "$app_name" "run single flow"

	while IFS= read -r line; do
		_h3 "$app_name" "flow" "$line"
	done < pipe &

	# shellcheck disable=SC2086  # platform_args must word-split, and vanish when empty
	maestro --verbose --no-ansi --platform "$platform" test --flatten-debug-output --debug-output="$HOME/.maestro/tests/${app_name}/flow" $platform_args "$target_yaml" 1>pipe 2>&1 \
		|| { _h2 "$app_name" "FAIL! Single-flow run failed"; ALL_PASS=false; }
}

if [ "$platform" = "web" ]; then
	# Prints the pid only when it starts one, so _cleanup stops a server we started and leaves
	# alone one that was already running.
	fixtures_pid="$(./ensure_fixtures)" || exit 1
fi

if [ -n "$flow_path" ]; then
	###
	### Single-flow mode: run exactly one yaml, no tag filtering.
	###
	app_name="${app_filter:-demo_app}"
	_run_single_flow "$app_name" "$flow_path"
else
	###
	### Every workspace the manifest lists, or just the one MAESTRO_APP names.
	###
	# Read from the file rather than a pipeline, so the loop body stays in this shell and can
	# still set ALL_PASS.
	matched=""

	while read -r app_name workspace_path platforms failing; do
		# Skip the manifest's comments and blank lines: read puts the first field of a comment
		# line in app_name, and leaves every field empty for a blank one.
		case "$app_name" in '#'*|'') continue ;; esac

		if [ -n "$app_filter" ] && [ "$app_name" != "$app_filter" ]; then
			continue
		fi
		matched=1

		if ! _supports_platform "$platforms" "$platform"; then
			echo "skipping $app_name on $platform"
			continue
		fi

		workspace_dir="./$workspace_path"

		_h1 "run tests for app \"$app_name\" on platform \"$platform\""
		_run_passing "$app_name" "$workspace_dir"

		if [ "$failing" = "yes" ]; then
			_run_failing "$app_name" "$workspace_dir"
		fi
	done < "$manifest"

	if [ -n "$app_filter" ] && [ -z "$matched" ]; then
		echo "unknown workspace: $app_filter (not listed in $manifest)"
		exit 1
	fi
fi

if [ "$ALL_PASS" = "false" ]; then
	_h1 "FAILURE: some tests failed!"
	exit 1
else
	_h1 "SUCCESS: all tests passed!"
fi
