appId: com.example.example
---
- launchApp # For idempotence of sections

- extendedWaitUntil:
    timeout: 100
    visible:
      text: 'Swipe Test'

- extendedWaitUntil:
    timeout: 100
    notVisible:
      text: 'Non Existent Text'
